# 漂移插值预检 QA

- 预检契约先于数值运行写入。
- 10/10 格、2,000 对/格、40 区块、零删失。
- 同锚点五个 λ 的增量哈希一致；漂移仿射残差 < 5e-17。
- 10/10 同时区间包含零；最大半宽 0.12822170。
- 预检不足以判断单调性或曲率；图中折线仅作规划展示。
- 正式样本量冻结为 36,560 对/格；正式与复跑种子均与预检分离。
- 输出格式遵循 PNG-only 偏好。
