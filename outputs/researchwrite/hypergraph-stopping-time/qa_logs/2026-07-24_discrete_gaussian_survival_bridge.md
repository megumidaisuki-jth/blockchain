# 离散—Gaussian 生存序桥接 QA

日期：2026-07-24  
范围：T19 证明桥接、三节点路径确定性 Poisson 验证、解析独立 Brownian 基准、PNG 图件、证据登记与全项目回归。

## 1. 数学审计

1. 相关模型和独立边际代理分别满足有界 i.i.d. FCLT；允许相关协方差退化，但每个边界法向方差必须严格为正。
2. 退出映射连续性使用首次接触后的逐面立即穿越；多个面同时接触只涉及有限 active set，不要求法向独立。
3. 平方势函数对两个模型具有同一正增量 `trace(Sigma)`；全状态 \(O(N^2)\) 均值界经强 Markov 分块升级为统一正指数矩，足以交换所有固定正阶矩。
4. Gaussian 相关不等式只用于中心对称二元通道安全区间；非中心初态、非零漂移和高阶单纯形均未外推。
5. 最终结论是极限随机序及归一化均值差极限非负，不是有限 \(N\) 符号定理。

## 2. 确定性验证

- 设计：三节点路径、均匀有序 OD、两条平衡二元通道。
- 相关协方差：`[[2/3, 1/3], [1/3, 2/3]]`；独立代理协方差：`diag(2/3,2/3)`。
- 尺度：\(N=1,2,4,8,16,32,64,128,256\)，未删除任何尺度。
- 方法：逐尺度以双精度稀疏线性求解器确定性求解被杀游走的 Poisson 方程，无 Monte Carlo 抽样；线性残差不单独构成严格前向误差证书。
- 最大线性残差：`1.0550138540565968e-10`。
- 独立 Brownian 解析谱级数：`0.8840562393751427`；2000/4000 项差：`2.1164070496126897e-11`。
- 独立离散外推与解析基准差：约 `2.37e-12`。
- 相关极限主规格外推：`0.9332361050440836`；多窗口/次数包络约 `0.93323207–0.933236114`，正文只报告约 `0.933236`。
- 极限差估计：约 `0.04918`，不把末位作为严格误差条。
- 有限尺度反例：\(N=1\) 的精确归一化差为 `-0.125`；全部受测 \(N\ge2\) 为正。
- 8/8 预设门禁通过。

## 3. 实现与回归

- 专项测试：补救后 6/6 通过。
- 全项目测试：补救后 103/103 通过，55.886 秒。
- 结果工件审计：35 份 `SHA256SUMS`/`SHA256SUMS.txt`，479 个文件，0 个哈希失败。
- `state.json` 可解析；进度 `78.0`，`publication_readiness=false`。

## 4. 图件 QA

- Python/matplotlib 单一后端；183 mm × 86 mm；600 dpi；PNG only。
- 原始 9 个尺度全部进入两面板图，无抽样、平滑或缺失值删除。
- 视觉检查通过：坐标、图例、直接极限标签、\(N=1\) 反例箭头和面板标签清晰，无遮挡。
- 自动预检：语法、字体、字号、颜色、DPI、183 mm 宽度、无抽样/无删除和后端一致性通过。
- 自动工具仍对缺少 SVG/PDF 报 1 个 FAIL、对缺少 TIFF 报 1 个 WARN；这是作者持续有效的 PNG-only 输出偏好所致，不是未执行的图件任务。

## 5. 状态判定

T19 可标为 **A—内部闭合、外部概率论评审未签署**。C3 的“退出映射 + 一致可积性”内部技术债已清除，但这不等于同行评审完成，也不使稿件进入可投稿状态。

其后隔离子 agent 给出独立对抗性验收 `ACCEPT`：0 个阻断问题、5 个非阻断问题；NB-1 至 NB-5 经主 agent 补救后由同一验收 agent 只读复核为 5/5 `CLOSED`、0 `OPEN`。该验收不等于外部人类概率论专家签核。
