# T34 独立验收与非阻断补救记录

**日期**：2026-07-24  
**验收者**：与 T34 原稿形成隔离的子 agent（只读验收）  
**最终结论**：`ACCEPT`；0 项阻断；5 项非阻断改进全部关闭。

## 1. 独立复核结果

验收者重新检查了：

1. \(\tau_0\) 为已接受路由执行后的首次触边，\(\rho\) 为请求执行前的首次拒绝；
2. 在严格合同 \(S_0\) 内，若余额型拒绝有限，则 \(\tau_0<\rho_{\rm bal}\)；
3. 不能强化为 \(\rho_{\rm bal}=\tau_0+1\)，因为反向流可先恢复余额，拒绝也可能不发生；
4. BOLT #2/#4/#7 与三篇原始研究足以支持现实机制无普适等式或先后序；
5. 92% 写作启动准备度与 84% 整体投稿进度不矛盾。

独立重跑得到 139,968 个规模—请求序列案例、110,308 个触边案例、71,688 个有限余额拒绝案例和 0 个索引违例；T34 专项测试 5/5 通过，发布结果清单 2/2 哈希通过。

## 2. 非阻断问题与关闭方式

| 编号 | 问题 | 补救 | 状态 |
|---|---|---|---|
| N1 | 穷举循环用 `assert` 作为违例门，`python -O` 可删除 | 改为显式累计 `violations`，非零立即抛出 `RuntimeError` | CLOSED |
| N2 | `boundary_time` 对非正初态返回 0，与 \(S_0\) 严格正初态不一致 | 非正初态改为显式 `ValueError`，并加入现有测试方法 | CLOSED |
| N3 | BOLT #2 的费用措辞未体现通道类型与费用责任条件 | 合同与来源表统一增加条件限定 | CLOSED |
| N4 | BOLT 链接指向可变 `master` | 固定到核验提交 `94eb038c42e664dd7862faeec6508ccd25f63ff8` | CLOSED |
| N5 | T35 与复现包仍引用 T32 阶段旧计数 | 更新为 113 测试、29 结果清单、825 哈希；T34 已并入 | CLOSED |

## 3. 最终边界

验收确认的是定义、索引、协议来源与写作边界的内部一致性，不是对真实 Lightning 支付失败时间的经验验证。本文仍必须把交易级余额、尝试、失败码、重试和再平衡日志缺失列为外部效度限制。
